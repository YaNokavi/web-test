// "use strict";

// let tg = window.Telegram.WebApp;

// tg.setHeaderColor("#1468B1");
// tg.expand();

// var userIdData = "User ID: " + `${tg.initDataUnsafe.user.id}`;
// var logoname = `${tg.initDataUnsafe.user.username}`[0] + `${tg.initDataUnsafe.user.username}`[1];
// var username = `${tg.initDataUnsafe.user.username}`;

// sessionStorage.setItem("userIdData", userIdData);
// sessionStorage.setItem("logoname", logoname);
// sessionStorage.setItem("username", username);
